# [QUAN TRỌNG] Copy thư mục thư viện 'lasotuvi' vào package
if [ -d "lasotuvi" ]; then
  echo "Found lasotuvi directory, copying..."
  cp -r lasotuvi package/
else
  echo "WARNING: lasotuvi directory NOT FOUND in $(pwd)"
  ls -la
  exit 1
fi